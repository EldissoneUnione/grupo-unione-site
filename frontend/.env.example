# Servidor do frontend (frontend/server.js)
# Copiar para .env e ajustar por ambiente.

# Porta onde o servidor de páginas escuta.
PORT=4000

# Interface de escuta. Em produção, atrás do nginx, normalmente localhost.
HOST=localhost

NODE_ENV=development
